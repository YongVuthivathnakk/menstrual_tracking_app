import 'package:flutter/material.dart';

class LogPeriodCard extends StatelessWidget {
  const LogPeriodCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(20),
      child: const Placeholder(),
    );
  }
}
