import 'package:flutter/material.dart';

class DefaultCalandar extends StatelessWidget {
  const DefaultCalandar({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(20),
      child: const Placeholder(),
    );
  }
}
